import type { NextPage } from 'next';
import { useEffect } from 'react';
import Head from 'next/head';
import { motion, useAnimation } from 'framer-motion';
import BannerVideo from '../src/components/BannerVideo';
import Characters from '../src/components/Characters';
import ImaginaryValues from '../src/components/ImaginaryValues';
import Why from '../src/components/Why';
import ImaginaryTeam from '../src/components/ImaginaryTeam';
import FAQ from '../src/components/FAQ';
import Community from '../src/components/Community';
import Good from '../src/components/Good';
import What from '../src/components/What';

const Home: NextPage = () => {
  const animation = {
    hidden: {
      opacity: 0,
    },
    visible: {
      opacity: 1,
    },
    exit: {
      opacity: 1,
    },
  };

  const controls = useAnimation();

  useEffect(() => {
    controls.start({
      scale: 15,
      transition: { duration: 1 },
    });
  }, []);

  return (
    <motion.div
      initial="hidden"
      animate="visible"
      exit="exit"
      variants={animation}
      className="page-container"
    >
      <Head>
        <title>Imaginary Ones</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <BannerVideo />
      <What />
      <Good />
      <Why />
      <Characters />
      <ImaginaryValues />
      <ImaginaryTeam />
      <FAQ />
      <Community />

      <motion.div className="loading-animation-screen fixed w-full h-full top-0 left-0 z-[100] pointer-events-none">
        <motion.img
          src="/final.svg"
          alt="animated-svg"
          className="rounded-full"
          initial={{ scale: 0 }}
          animate={controls}
        />
      </motion.div>
    </motion.div>
  );
};

export default Home;
